# Hackathon workspace — 薄索引

規則正本：`~/.agents/institution/CORE.md`（本檔不重複規則本體）。

本專案特化（黑客松模式）：

- 目標是**當天可 demo**，不是可維護性。最小方案優先加倍適用：能 hardcode 的 demo 資料就 hardcode，不建抽象層。
- 例外不放寬的：CORE §5 安全紅線、不 commit secrets（`.env` 已 ignore）。
- 對外發佈（deploy、開 public repo、交件）前先跟使用者確認一次。
