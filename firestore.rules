rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App is local-only. Deny all until Auth is added.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
