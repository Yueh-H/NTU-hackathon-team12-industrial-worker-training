rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // App is local-only. Deny all until Auth is added.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
